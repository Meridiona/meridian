{
  "name": "meridian-release-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Release tooling for Meridian (semantic-release). Not published — see npm/meridian for the distributed package.",
  "license": "MIT",
  "devDependencies": {
    "semantic-release": "^24.2.3",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
