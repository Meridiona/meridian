name: Publish

# Stage 2 of the two-stage release pipeline.
#
# Triggered by a tag push from Stage 1 (release.yml). Checks out the tagged
# commit, builds the macOS arm64 payload, publishes both npm packages via
# OIDC trusted publishing, uploads the binary asset to the draft GitHub
# release, and converts it to published.
#
# Retry-safe: re-running this workflow for the same tag is safe — npm rejects
# already-published versions gracefully, asset upload is idempotent, and
# gh release edit is idempotent. Use workflow_dispatch with the tag input to
# re-publish a failed release without a dummy commit.
#

on:
  push:
    tags:
      - "v[0-9]*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to publish (e.g. v1.2.3)"
        required: true

permissions:
  contents: write
  id-token: write   # OIDC trusted publishing — no NPM_TOKEN needed

jobs:
  publish:
    runs-on: macos-26
    env:
      SQLX_OFFLINE: "true"
      MERIDIAN_JIRA_OAUTH_CLIENT_SECRET: ${{ secrets.JIRA_OAUTH_CLIENT_SECRET }}
    steps:
      - name: Resolve tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "name=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.tag.outputs.name }}
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Install Rust 1.93.1
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.1"
      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: |
            package-lock.json
            ui/package-lock.json
            tray/package-lock.json

      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: ui/.next/cache
          key: nextjs-${{ hashFiles('ui/package-lock.json') }}-${{ hashFiles('ui/**/*.ts', 'ui/**/*.tsx') }}
          restore-keys: |
            nextjs-${{ hashFiles('ui/package-lock.json') }}-
            nextjs-

      - name: Cache Node.js 22 arm64 tarball
        uses: actions/cache@v4
        with:
          path: ~/.cache/node22-arm64
          key: node22-22.22.3-darwin-arm64

      - name: Install release tooling
        run: npm ci --no-audit --no-fund

      - name: Set version
        run: bash scripts/set-version.sh "${{ steps.tag.outputs.name }}"

      - name: Build
        run: |
          VERSION="${{ steps.tag.outputs.name }}"

          # Rust and UI are independent — run them in parallel.
          # --workspace builds the daemon AND the tray binary in one cargo
          # invocation: shared deps compile once and the tray's tauri/wry crates
          # overlap the daemon build instead of running as a serial tail. The
          # tray binary lands at target/release/meridian-tray (the exact path
          # package-release.sh copies). We deliberately skip `tauri build`: its
          # only output we use is that bare binary — the .app/.dmg bundle is
          # discarded, the tray icon is include_bytes! (compile-time embedded),
          # all bundle icons are committed (so tauri_build::build() needs no
          # generated assets), and the binary is adhoc linker-signed by cargo
          # exactly as `tauri build` leaves it. So no tauri-cli npm ci, no
          # create-icons.sh, and no bundling on the critical path.
          cargo build --release --workspace &
          CARGO_PID=$!
          (cd ui && npm ci --no-audit --no-fund && npm run build) &
          UI_PID=$!
          wait $CARGO_PID || { echo "cargo build failed"; exit 1; }
          wait $UI_PID   || { echo "ui build failed"; exit 1; }

          bash scripts/package-release.sh "${VERSION#v}"
          bash scripts/verify-release-bundle.sh "${VERSION#v}"

      - name: Publish packages
        # Calls @semantic-release/npm directly — same OIDC auth path the old
        # single-stage workflow used, without running the full pipeline.
        run: |
          node --input-type=module - <<'EOF'
          const { verifyConditions, publish } = await import('@semantic-release/npm');
          const version = process.env.RELEASE_VERSION.replace(/^v/, '');
          const logger = { log: console.log, error: console.error, warn: console.warn };
          const ctx = {
            nextRelease: { version, channel: null, gitTag: 'v' + version, notes: '' },
            options: { publish: [{ path: '@semantic-release/npm' }] },
            logger, env: process.env, cwd: process.cwd(),
            stdout: process.stdout, stderr: process.stderr,
          };
          await verifyConditions({ npmPublish: true }, ctx);
          for (const pkgRoot of ['npm/meridian-darwin-arm64', 'npm/meridian']) {
            await publish({ pkgRoot, npmPublish: true }, ctx);
            console.log('published', pkgRoot);
          }
          EOF
        env:
          RELEASE_VERSION: ${{ steps.tag.outputs.name }}

      - name: Publish draft release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          # Convert draft → published (idempotent — safe to re-run on retry)
          gh release edit "${{ steps.tag.outputs.name }}" --draft=false --repo "$GITHUB_REPOSITORY"

      - name: Commit version bumps to main
        # Pushes the updated manifests (Cargo.toml, package.json files, etc.)
        # back to main so the repo reflects the released version. The [skip ci]
        # marker prevents this commit from triggering Stage 1 again.
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.tag.outputs.name }}"
          VERSION="${TAG#v}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add \
            Cargo.toml Cargo.lock \
            services/pyproject.toml \
            ui/package.json \
            packages/meridian-mcp/package.json \
            npm/meridian/package.json \
            npm/meridian-darwin-arm64/package.json

          # Only commit if there are staged changes (idempotent on retry)
          if ! git diff --cached --quiet; then
            git commit -m "chore(release): ${VERSION} [skip ci]"
            git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
            git push origin HEAD:main
          fi
