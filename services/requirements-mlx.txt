# MLX direct-inference dependencies for run_task_linker_mlx.py and server.py --backend mlx
#
# Requires Python 3.13 (outlines 1.x is incompatible with Python 3.14+).
# Install into a dedicated venv:
#
#   python3.13 -m venv .venv313
#   .venv313/bin/pip install -r requirements-mlx.txt -r requirements.txt
#
mlx-lm>=0.31,<1
outlines[mlxlm]>=1.3,<2
pydantic>=2.10,<3

# Server (agents/server.py --backend mlx)
fastapi>=0.115,<1
uvicorn>=0.30,<1
